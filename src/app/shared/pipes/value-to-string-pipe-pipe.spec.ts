import { ValueToStringPipe } from './value-to-string-pipe';

describe('ValueToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
