import { ReplaceByNrDirective } from './replace-by-nr.directive';

describe('ReplaceByNrDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceByNrDirective();
    expect(directive).toBeTruthy();
  });
});
